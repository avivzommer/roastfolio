{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo BOOT-1 ; echo \"PORT=$PORT\" ; echo \"NODE_ENV=$NODE_ENV\" ; echo \"DBURL_LEN=${#DATABASE_URL}\" ; echo BOOT-2 && node scripts/apply-migrations.mjs && echo BOOT-3 && exec ./node_modules/.bin/next start -H 0.0.0.0 -p 8080",
    "healthcheckPath": "/",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
